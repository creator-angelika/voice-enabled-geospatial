import React from 'react';
import './styles.css';

const Hero = () => {
  return (
    <div className="hero">
      <h1>Voice-Enabled Map Interface Prototype - Team Galatea</h1>
      <p>
        This prototype demonstrates a voice-enabled user interface for geospatial map-based applications.
        You can use voice commands to interact with the map, such as zooming in, zooming out, and displaying highways in different states.
      </p>
    </div>
  );
};

export default Hero;